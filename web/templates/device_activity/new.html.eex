<h2>New device activity</h2>

<%= render "form.html", changeset: @changeset,
                        action: device_activity_path(@conn, :create) %>

<%= link "Back", to: device_activity_path(@conn, :index) %>
